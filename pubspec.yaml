name: mk8_randomizer
description: Mario Kart 8 Randomizer
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  dynamic_height_grid_view: ^0.0.4
  flutter_riverpod: ^2.6.1
  riverpod_annotation: ^2.6.1
  shared_preferences: ^2.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.15
  flutter_lints: ^5.0.0
  riverpod_generator: ^2.6.4
  riverpod_lint: ^2.6.5
  custom_lint:

flutter:
  uses-material-design: true

  assets:
    - images/
